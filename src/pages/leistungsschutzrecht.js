import React from 'react';
import { Link } from 'gatsby';
import Layout from '../components/layout';

export default ({ data }) => (
	
  <Layout>
    <h1>Zum Leistungsschutzrecht (in Deutschland und der EU)</h1>
    <p>Hier erscheint demnächst ein Text...</p>
  </Layout>
)